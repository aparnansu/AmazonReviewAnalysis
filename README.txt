Main program is start_project.py
#Dataset file is not present here. It can be downloaded from this link: http://snap.stanford.edu/data/web-Amazon-links.html.
Include this path in "dirPath" variable in the program.

List of options:
# List of all datasets (Takes longer duration)
#dirPath = "Datasets"
# Small dataset to realize the result
dirPath = "Small_dataset" (Baby.txt.gz is the fastest that returns execution within a few seconds)

# Option to plot a chart
plotGraph = True

# Option to aggregate the top rated info on all the categories
printReport = False

# Option for holding the number of products in memory
maxProducts = 1000000
